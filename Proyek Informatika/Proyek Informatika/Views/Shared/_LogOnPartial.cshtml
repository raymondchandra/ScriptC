@if(Request.IsAuthenticated) {
    var login = (string)Session["role"]; 
    <text><strong>@User.Identity.Name</strong> - @login
    @Html.ActionLink("[ Log Off ]", "LogOff", "Account")</text>
}
else {
    @:@Html.ActionLink("[ Log On ]", "LogOn", "Account")
}
