<script src="../../../Scripts/jquery-ui.js" type="text/javascript"></script>
<script type="text/javascript">
    $(function () {
        $("#tabs").tabs();
    });
</script>
<script type="text/javascript">
    function viewDetails2(e) {
        var detailWindow = $("#Details2").data("tWindow");
        detailWindow.center().open();

    }
</script>

            @(Html.Telerik().Grid<Proyek_Informatika.Models.KoordinatorPenilaian>()
                .Name("Grid2")
                .Columns(columns =>
                {
                    columns.Bound(o => o.npm).Width(100);
                    columns.Bound(o => o.nama).Width(100);
                    columns.Bound(o => o.judul).Width(200);
                    columns.Bound(o => o.nilaiAkhir).Width(70);
                     columns.Command(commands => commands
                                .Custom("viewDetails2")
                                .Text("Detail Nilai")
                                //.DataRouteValues(route => route.Add(o => o.id).RouteKey("id"))
                                .Ajax(true))
                                //.Action("_SelectDetailSkripsi2", "Nilai"))
                            .HtmlAttributes(new { style = "text-align: center" })
                            .Width(150);
                })
                .ClientEvents(events => events.OnCommand("viewDetails2"))
                .DataBinding(dataBinding => dataBinding.Ajax().Select("_SelectNilaiSkripsi2", "Nilai"))
                .Pageable()
                .Sortable()
                .Filterable()
            )

            @(Html.Telerik().Window()
                .Name("Details2")
                .Visible(false)
                .Title("Detail Nilai")
                .Modal(true)
                .Width(500)
                .Height(500)
                .Scrollable(true)
                .LoadContentFrom("DetailNilaiSkripsi2","Nilai"))
